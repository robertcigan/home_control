<% if @panel.errors.any? || @reload %>
  <%= ajax_modal_render("form") %>
<% else %>
  if ($("#panel_search").length) {
    <%= refresh_list_by_script("#panel_search") %>
  }
  $("#ajax-modal").modal("hide")
  <%= show_flash %>
<% end %>
