<% if @device.errors.any? || @reload %>
  <%= ajax_modal_render("form") %>
<% else %>
  if ($("#device_search").length) {
    <%= refresh_list_by_script("#device_search") %>
  }
  $("#ajax-modal").modal("hide")
  <%= show_flash %>
<% end %>
