<% if @board.errors.any? || @reload %>
  <%= ajax_modal_render("form") %>
<% else %>
  if ($("#board_search").length) {
    <%= refresh_list_by_script("#board_search") %>
  }
  $("#ajax-modal").modal("hide")
  <%= show_flash %>
<% end %>
